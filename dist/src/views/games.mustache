<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Competitions</title>
</head>

<body>

    {{> header}}
    <div class="container">
        <h1>Games</h1>
        <form role="search" id="form_search_game" method="GET">
            <input type="search" class="form-control" placeholder="Search Date" aria-label="Search" id="input_search_game">
        </form>
        <a href="/view/teams">Reset</a>
        <br>
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th scorpe="col">Competition</th>
                    <th scorpe="col">Season</th>
                    <th scope="col">Home Team</th>
                    <th scope="col">Score</th>
                    <th scope="col">Away Team</th>
                    <th scorpe="col">Date</th>
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
            {{#games}}
                <tr class="align-middle">
                    <td>{{competition_name}}</td>
                    <td>{{competition_season}}</td>
                    <td>{{house_team}}</td>
                    <td>{{game_result}}</td>
                    <td>{{visiting_team}}</td>
                    <td>{{game_date}}</td>
                    <!-- TODO: PEGAR O NOME DAS TEAMS, TORNAR AS TEAMS EM BUTTONS-->-
                    <td>
                        <button type="button" onclick="showDetails({{game_id}})" class="btn btn-warning" data-bs-toggle="modal" style="color:#fff;">Details</button>
                    </td>
                </tr>
            {{/games}}
            </tbody>
        </table>
    </div>
    </body>
</html>